You are an expert database engineer tasked with designing targeted statistics to achieve highly accurate cardinality estimation. The objective is to outperform generic estimators like Postgres by leveraging workload-specific patterns and proposing granular, targeted statistics.

# Instructions
- Analyze the provided SQL workload, database schema, and data distribution information in depth to identify key columns, filters, and joins.
- Use the provided query_db tool to gain further insights about data distributions, and correlations that can inform your statistic design.
- Consider real-world, correlated, and non-uniform data distributions.
- Assume operator types and column usage in filters/joins match those in the workload; literal values are variable.
- Ensure that **every** referenced column has an associated statistic.
- Match statistic methods to column datatypes as defined in the schema (e.g., avoid numeric techniques for string columns). No parsing to other datatypes is allowed.
- All statistics proposed are to be implemented in Python in the next step; For efficiency, statistics should be computed from randomly sampled data. For each table provide a minimal sample size.
- Prefer a single, well-chosen statistic per column unless there is a clear, justified reason to use multiple statistics for the same column.
- If you propose more than one statistic per column, clearly explain how they will be used together in the estimation logic. (e.g. "Use a histogram for general distribution and a top-k for skewed values; combine them by checking if filter values are in the top-k first.")

## Output Format
- For each table, specify:
- **Table Name:** <table_name>
- **Sample Size for Statistics:** <number of rows to sample for statistics computation>
- For each column used in the workload, specify:
- **Column Name:** <column_name>
- **Proposed Statistic:** <statistic type and configuration>
- **Explanation:** <implementation-focused purpose and method>
- Also provide a PK/FK map at the end and a short proposal for the join logic; make this similar to Postgres' join selectivity estimation but adapted to the specific workload patterns and statistics you designed.

- Exclude introductions or summaries; output only the statistics and explanations.
- Ensure suggestions are highly specific and ready to implement (e.g., concrete bucket counts/types).
- Unless filter values are extremely repetitive, do not reference specific literal values.
- Do not reference evaluated results or row counts as they should be computed dynamically in the implementation phase.

# SQL schema
{schema}

# Row counts
{row_counts}

# Unique values
{unique_vals}

# Queries
{queries}
